{% extends "_layout_profile.twig" %}

{% set title = common.username %}

{% block scripts %}
    <script>
        comment_type = 'profile';
        comment_id = '{{ common.id }}'
        user_id = '{{ common.id }}'
    </script>
{% endblock %}

{% block precontentappend %}
    {% if data.is_current %}
    <div class="profile-edit-container">
        <div class="profile-edit">
            <div class="button secondary">Settings</div>
            <div class="button secondary">Colors</div>
            <div class="text-danger">Subject to change</div>
        </div>
    </div>
    {% endif %}
{% endblock %}

{% block content %}
{% endblock %}