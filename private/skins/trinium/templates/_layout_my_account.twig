{% extends "_layout.twig" %}

	{% block content %}
        <div class="section">
            <div class="section-header flex">
                <div class="section-header-left">
                    <nav class="section-header-crumbs">
                        <ol>
                            <li><a href="/my_account">{{ localize('my_account') }}</a></li>
                            <li><a href="#">{{ title }}</a></li>
                        </ol>
                    </nav>
                </div>
                <div class="section-header-right">
                    Right
                </div>
                {#<div class="section-header-left">
                    <h1 class="section-title">{{ localize('my_account') }}</h1>
                    <div class="section-desc">{{ localize('page_num_of_num', current_page, total_pages )}}</div>
                </div>#}
            </div>
            {% block my_account_content %}
            {% endblock %}
        </div>
    {% endblock %}