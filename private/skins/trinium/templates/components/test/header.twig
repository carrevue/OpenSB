<div class="header">
    <h1>
        <a href="/">{{ website_branding.name }}</a>
    </h1>
    {% for link in sidebar_main_links() %}
        <a href="{{ link.url }}">{{ link.name }}</a>
    {% endfor %}
    {% for link in header_user_links() %}
        <a href="{{ link.url }}">{{ link.name }}</a>
    {% endfor %}
    <br>
    {% if is_user_logged_in %}
        {% for link in header_user_switcher_links() %}
            <a href="/login/{{ link.name }}">Login as {{ link.name }}</a>
        {% endfor %}
    {% endif %}
</div>