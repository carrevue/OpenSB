<footer class="footer">
    <div class="footer-container">
        <div class="footer-powered-by">
            <span>{{ localize('powered_by') }} OpenSB {{ opensb_version.name }} {{ opensb_version.number }}</span>
        </div>
        <div class="footer-links">
            {% for link in footer_links() %}
                <span><a href="{{ link.url }}">{{ link.name }}</a></span>
            {% endfor %}
        </div>
        <div class="footer-copyright">
            <span>{{ localize('footer_copyright', ('now' | date('Y'))) }}</span>
        </div>
        {% if is_debug %}
            <div>
                <a class="button danger small" id="debug-button">{{ localize('debug') }}</a>
            </div>
        {% endif %}
    </div>
</footer>