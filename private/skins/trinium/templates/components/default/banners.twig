{% if session.notif_message %}
    <div class="banner banner-{{ session.notif_color }}">
        {{ notification_icon(session.notif_color) }}
        <span class="banner-text">
        {{ session.notif_message }}
        </span>
    </div>
    {{ remove_notification() }}
{% endif %}