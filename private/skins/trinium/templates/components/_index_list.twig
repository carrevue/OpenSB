{% if data.uploads_following %}
    <div class="section">
        <div class="section-header flex">
            <div class="section-header-left">
                <h2 class="section-title">{{ localize('following') }}</h2>
                <div class="section-desc">{{ localize('following_uploads_desc') }}</div>
            </div>
            <div class="section-header-right">
                <a href="/browse">{{ localize('more') }}</a>
            </div>
        </div>
        <div class="content-box">
            {% for upload in data.uploads_following %}
                {{ include('/components/_upload_vertical.twig') }}
            {% endfor %}
        </div>
    </div>
{% elseif data.uploads_new %}
<div class="section">
    <div class="section-header flex">
        <div class="section-header-left">
            <h2 class="section-title">{{ localize('homepage_recent_uploads') }}</h2>
            <div class="section-desc">{{ localize('homepage_recent_uploads_desc') }}</div>
        </div>
        <div class="section-header-right">
            <a href="/browse">{{ localize('more') }}</a>
        </div>
    </div>
    <div class="content-box">
        {% for upload in data.uploads_new %}
            {{ include('/components/_upload_vertical.twig') }}
        {% endfor %}
    </div>
</div>
{% endif %}