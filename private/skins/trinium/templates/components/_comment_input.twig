{% if is_user_logged_in %}
<div class="new-comment" id="comment_field">
    <div class="profile-picture">
        <img src="{{ profile_picture(user_data.id) }}" class="pfp" alt="{{ user_data.name }}">
    </div>
    <div id="new-comment-button">
        {{ localize('write_comment') }}
    </div>
    <div id="new-comment-form">
        <textarea class="form-submit" id="comment_contents" placeholder="{{ localize('write_comment') }}" name="comment" maxlength="1000" rows="3"></textarea>
        <div class="new-comment-bottom">
            <div class="text-danger comment-form-error" id="comment-form-error"></div>
            <div class="new-comment-buttons">
                <button type="cancel" id="comment_cancel_button" name="cancel" class="button secondary">
                    {{ localize('cancel') }}
                </button>
                <button type="submit" id="comment_post_button" name="post" class="button primary">
                    {{ localize('post_action') }}
                </button>
            </div>
        </div>
    </div>
</div>
{% else %}
<div id="new-comment-button">
    {{ localize('please_login_to_comment') }}
</div>
{% endif %}