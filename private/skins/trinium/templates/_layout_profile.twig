{% extends "_layout.twig" %}

{% block style %}
        <style>
        :root {
            {% if data.customization %}
                --bg-page: {{ data.customization.background_color }};
                --link-color: {{ data.customization.link_color }};
                --title-color: {{ data.customization.title_color }};

                --highlight-bg: {{ data.customization.highlight_box_background_color }};
                --highlight-border: {{ data.customization.highlight_box_border_color }};
                --highlight-text: {{ data.customization.highlight_box_text_color }};

                --basic-bg: {{ data.customization.basic_box_background_color }};
                --basic-border: {{ data.customization.basic_box_border_color }};
                --basic-text: {{ data.customization.basic_box_text_color }};

                {% if data.customization.font %}
                --page-font: {{ data.customization.font | raw }};
                {% endif %}
            {% endif %}
                --username-color: {{ data.color }};
        }

        {% if banner_picture %}
        .jumbotron.profile-banner {
        {% if data.customization %}
            background-color: {{ data.customization.background_color }};
        {% endif %}
            background-image: url('{{ banner_picture }}');
        }
        {% endif %}

        {% if data.customization %}
        body {
            background: var(--bg-page);
        }
        
        /* Font */
        .page {
            font-family: var(--page-font, inherit);
        }

        /* Links */
        a {
            color: var(--link-color);
        }

        /* Titles */
        .section-title {
            color: var(--title-color);
        }

        /* Highlight box */
        .content-box.highlight-box {
            background-color: var(--highlight-bg);
            border-color: var(--highlight-border);
            color: var(--highlight-text);
        }

        .content-box.highlight-box hr,
        .profile-details .profile-details-list .detail-item {
            border-color: var(--highlight-border);
        }

        /* Basic box */
        .content-box.basic-box {
            background-color: var(--basic-bg);
            border-color: var(--basic-border);
            color: var(--basic-text);
        }

        .content-box.basic-box hr,
        .content-box.basic-box .content-entry,
        .content-box.basic-box .comment,
        .content-box.basic-box .replies,
        .content-box.basic-box .new-comment,
        .content-box.basic-box .reply-form {
            border-color: var(--basic-border);
        }

        /* Footer */
        .footer .footer-container {
            padding-top: 1em;
            color: var(--basic-text);
            border-top: 1px solid var(--basic-border);
        }

        .footer .footer-container .footer-copyright {
            opacity: 1;
        }

        /* Comments/Userlink */
        .comment .comment-body .comment-author .comment-timestamp,
        .userlink-container .userlink .userlink-handle {
            color: var(--basic-text);
            opacity: 1;
        }

        .userlink-container .userlink a {
            color: var(--basic-text) !important;
        }
        {% endif %}
        
        .profile-box-username {
            color: var(--username-color);
        }
    </style>
{% endblock %}

{% block precontent %}
    {% if banner_picture %}
    <div class="jumbotron profile-banner {% if options.trinium_show_sidebar %}sidebar-active{% endif %}" id="profile-banner"></div>
    {% endif %}
{% endblock %}

{% block content %}{% endblock %}
